# Mock-data mode: 1 = every API module returns in-memory data seeded from the mockup.
# Set to 0 (or delete this line) to hit the real backend through the Vite proxy (/api -> http://localhost:8000).
VITE_USE_MOCKS=1
