/* Admin sections for the printed prescription (lane R fills them in, session 4): print settings
   (id "h-rxprint"), exam findings (id "h-exam") and glass lens types (id "h-lenstypes").
   Rendered from Admin.jsx. */
// eslint-disable-next-line no-unused-vars
export function RxPrintAdminSections({ run }) {
  return null;
}
