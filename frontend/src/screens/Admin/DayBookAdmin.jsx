/* Admin sections for the day book (lane M fills them in, session 4): the day-book columns list
   (id "h-heads"). Rendered from Admin.jsx. */
// eslint-disable-next-line no-unused-vars
export function DayBookAdminSections({ run }) {
  return null;
}
