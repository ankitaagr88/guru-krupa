services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: gurukrupa
      POSTGRES_PASSWORD: gurukrupa
      POSTGRES_DB: gurukrupa
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
